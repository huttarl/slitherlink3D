[pytest]
markers =
    slow: thorough tests skipped in default runs (see conftest.py; run with --all or -m slow)
; Default runs skip tests marked 'slow' (implemented in conftest.py).
;     pytest --all util/tests     -> run ALL tests, including slow ones
;     pytest -m slow util/tests   -> run only the slow tests
