[pytest]
markers =
    slow: thorough tests excluded from default runs (run with: pytest -m slow util/tests)
; Default runs skip tests marked 'slow'. To run ONLY the slow tests:
;     pytest -m slow util/tests
; To run everything:
;     pytest -m "slow or not slow" util/tests
addopts = -m "not slow"
